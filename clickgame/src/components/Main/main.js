import React from 'react';
import './main.css';
import Card from '../cards';
// import picCard from './cards.json';

const Main = () =>(
  <div className="main-container">
      <Card
      // alt={picCard[0].name} src={picCard.image} 
      />
  </div>
);
export default Main;